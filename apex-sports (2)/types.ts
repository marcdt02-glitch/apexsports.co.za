export interface ServiceLink {
  title: string;
  description: string;
  imageAlt: string;
  imageUrl: string;
  path: string;
}

export interface ValueItem {
  title: string;
  description: string;
}

export interface Testimonial {
  id: number;
  name: string;
  role: string;
  quote: string;
}